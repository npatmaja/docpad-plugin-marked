// Generated by CoffeeScript 1.8.0
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  module.exports = function(testers) {
    var MyTester;
    return MyTester = (function(_super) {
      __extends(MyTester, _super);

      function MyTester() {
        return MyTester.__super__.constructor.apply(this, arguments);
      }

      MyTester.prototype.docpadConfig = {
        enabledPlugins: {
          'marked': true
        },
        plugins: {
          marked: {
            markedRenderer: {
              code: function(code, lang) {
                var escape, escaped, out;
                escape = function(html, encode) {
                  var pattern;
                  pattern = !encode ? /&(?!#?\w+;)/g : /&/g;
                  return html.replace(pattern, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;').replace(/'/g, '&#39;');
                };
                if (code.match(/^sequenceDiagram/) || code.match(/^graph/)) {
                  return "<div class=\"mermaid\">\n" + code + "\n</div>\n";
                } else {
                  if (this.options.highlight) {
                    out = this.options.highlight(code, lang);
                    if (out !== null && out !== code) {
                      escaped = true;
                      code = out;
                    }
                  }
                  if (!lang) {
                    return '<pre><code>' + (escaped ? code : escape(code, true)) + '\n</code></pre>';
                  }
                  return '<pre><code class="' + this.options.langPrefix + escape(lang, true) + '">' + (escaped ? code : escape(code, true)) + '\n</code></pre>\n';
                }
              }
            }
          }
        }
      };

      return MyTester;

    })(testers.RendererTester);
  };

}).call(this);
